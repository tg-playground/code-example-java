services:
  # A Kafka broker service in KRaft mode also acts as a controller
  kafka1:
    image: "apache/kafka:4.1.1"
    ports:
      - '10001:9092'
    environment:
      # Use the same cluster id for all brokers
      # Generated by `docker run --workdir /opt/kafka/bin/ -it apache/kafka:latest ./kafka-storage.sh random-uuid`
      KAFKA_CLUSTER_ID: Spx7-44WSRSKpMceBdEWZw
      # Each broker must have a unique ID
      KAFKA_NODE_ID: 1
      # Roles for KRaft: broker, controller, or both
      KAFKA_PROCESS_ROLES: broker,controller
      # Internal and controller listeners
      KAFKA_LISTENERS: "INTERNAL://kafka1:29092,EXTERNAL://0.0.0.0:9092,CONTROLLER://kafka1:9093"
      # Advertised listener is what clients use to connect
      KAFKA_ADVERTISED_LISTENERS: "INTERNAL://kafka1:29092,EXTERNAL://localhost:10001"
      # Maps listener names to security protocols
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: "CONTROLLER:PLAINTEXT,INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT"
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_INTER_BROKER_LISTENER_NAME: INTERNAL
      # Quorum voters define all nodes in the KRaft quorum
      KAFKA_CONTROLLER_QUORUM_VOTERS: "1@kafka1:9093,2@kafka2:9093,3@kafka3:9093"
      # Storage Configuration
      KAFKA_LOG_DIRS: "/var/lib/kafka/data"
    volumes:
      - kafka1-data:/var/lib/kafka/data
    healthcheck:
      #      test: [ "CMD-SHELL", "netstat -tlnp | grep :9092" ]
      test: [ "CMD", "bash", "-c", "echo > /dev/tcp/localhost/9092" ]
      interval: 5s
      timeout: 5s
      retries: 5
  kafka2:
    image: "apache/kafka:4.1.1"
    ports:
      - '10002:9092'
    environment:
      # Use the same cluster id for all brokers
      # Generated by `docker run --workdir /opt/kafka/bin/ -it apache/kafka:latest ./kafka-storage.sh random-uuid`
      KAFKA_CLUSTER_ID: Spx7-44WSRSKpMceBdEWZw
      # Each broker must have a unique ID
      KAFKA_NODE_ID: 2
      # Roles for KRaft: broker, controller, or both
      KAFKA_PROCESS_ROLES: broker,controller
      # Internal and controller listeners
      KAFKA_LISTENERS: "INTERNAL://kafka2:29092,EXTERNAL://0.0.0.0:9092,CONTROLLER://kafka2:9093"
      # Advertised listener is what clients use to connect
      KAFKA_ADVERTISED_LISTENERS: "INTERNAL://kafka2:29092,EXTERNAL://localhost:10002"
      # Maps listener names to security protocols
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: "CONTROLLER:PLAINTEXT,INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT"
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_INTER_BROKER_LISTENER_NAME: INTERNAL
      # Quorum voters define all nodes in the KRaft quorum
      KAFKA_CONTROLLER_QUORUM_VOTERS: "1@kafka1:9093,2@kafka2:9093,3@kafka3:9093"
      # Storage Configuration
      KAFKA_LOG_DIRS: "/var/lib/kafka/data"
    volumes:
      - kafka2-data:/var/lib/kafka/data
    healthcheck:
      #      test: [ "CMD-SHELL", "netstat -tlnp | grep :9092" ]
      test: [ "CMD", "bash", "-c", "echo > /dev/tcp/localhost/9092" ]
      interval: 5s
      timeout: 5s
      retries: 5
  kafka3:
    image: "apache/kafka:4.1.1"
    ports:
      - '10003:9092'
    environment:
      # Use the same cluster id for all brokers
      # Generated by `docker run --workdir /opt/kafka/bin/ -it apache/kafka:latest ./kafka-storage.sh random-uuid`
      KAFKA_CLUSTER_ID: Spx7-44WSRSKpMceBdEWZw
      # Each broker must have a unique ID
      KAFKA_NODE_ID: 3
      # Roles for KRaft: broker, controller, or both
      KAFKA_PROCESS_ROLES: broker,controller
      # Internal and controller listeners
      KAFKA_LISTENERS: "INTERNAL://kafka3:29092,EXTERNAL://0.0.0.0:9092,CONTROLLER://kafka3:9093"
      # Advertised listener is what clients use to connect
      KAFKA_ADVERTISED_LISTENERS: "INTERNAL://kafka3:29092,EXTERNAL://localhost:10003"
      # Maps listener names to security protocols
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: "CONTROLLER:PLAINTEXT,INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT"
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_INTER_BROKER_LISTENER_NAME: INTERNAL
      # Quorum voters define all nodes in the KRaft quorum
      KAFKA_CONTROLLER_QUORUM_VOTERS: "1@kafka1:9093,2@kafka2:9093,3@kafka3:9093"
      # Storage Configuration
      KAFKA_LOG_DIRS: "/var/lib/kafka/data"
    volumes:
      - kafka3-data:/var/lib/kafka/data
    healthcheck:
      #      test: [ "CMD-SHELL", "netstat -tlnp | grep :9092" ]
      test: [ "CMD", "bash", "-c", "echo > /dev/tcp/localhost/9092" ]
      interval: 5s
      timeout: 5s
      retries: 5
  kafdrop:
    image: "obsidiandynamics/kafdrop:4.2.0"
    depends_on:
      kafka1:
        condition: service_healthy
      kafka2:
        condition: service_healthy
      kafka3:
        condition: service_healthy
    ports:
      - "9000:9000"
    environment:
      TZ: Asia/Shanghai
      KAFKA_BROKERCONNECT: "kafka1:29092,kafka2:29092,kafka3:29092"
      JVM_OPTS: "-Xms32M -Xmx64M"

volumes:
  kafka1-data:
  kafka2-data:
  kafka3-data:
